% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transformations.R
\name{rotate_table_around_axis}
\alias{rotate_table_around_axis}
\title{Rotate all points in a table around an axis}
\usage{
rotate_table_around_axis(
  table,
  rotation_axis,
  angle,
  point_on_axis = c(0, 0, 0),
  tol = 1e-08,
  zap = TRUE
)
}
\arguments{
\item{table}{A data frame or matrix containing columns \code{x}, \code{y}, \code{z}.}

\item{rotation_axis}{Numeric length-3 vector giving the axis direction
(need not be unit; will be normalized internally).}

\item{angle}{Numeric scalar (radians); rotation angle (right-hand rule).}

\item{point_on_axis}{Optional numeric length-3 vector for a point on the axis
(default \code{c(0,0,0)}).}

\item{tol}{Numeric tolerance for degeneracy checks.}

\item{zap}{Logical; if \code{TRUE}, zaps small numerical noise.}
}
\value{
The input \code{table} with rotated \code{x}, \code{y}, \code{z} columns.
}
\description{
Rotates each row of a coordinate table (with columns \code{x}, \code{y}, \code{z}) about a
specified axis by a given angle. The axis may pass through an arbitrary point
(default is the origin).
}
\examples{
pts <- data.frame(x = c(2, 3), y = c(0, 0), z = c(0, 0))
axis <- c(0, 0, 1)
pivot <- c(1, 0, 0)
rotate_table_around_axis(pts, axis, pi / 2, point_on_axis = pivot)

}
