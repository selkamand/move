% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shorthand.R, R/transformations.R
\name{\%angle\%}
\alias{\%angle\%}
\alias{measure_angle_between_vectors}
\title{Measure the angle between two vectors}
\usage{
a \%angle\% b

measure_angle_between_vectors(a, b, degrees = FALSE)
}
\arguments{
\item{a, b}{Numeric vectors of equal length.}

\item{degrees}{Logical; if \code{TRUE}, return the angle in degrees
instead of radians.}
}
\value{
Numeric scalar giving the angle between \code{a} and \code{b}.
Returns \code{NA} if either vector has zero magnitude.
}
\description{
Computes the angle between two numeric vectors using the dot product formula:
\deqn{\theta = \arccos\left( \frac{a \cdot b}{\|a\|\|b\|} \right)}
The angle can be returned in either radians (default) or degrees.
}
\examples{
measure_angle_between_vectors(c(1, 0, 0), c(0, 1, 0)) # pi/2 radians
measure_angle_between_vectors(c(1, 0, 0), c(0, 1, 0), degrees = TRUE) # 90 degrees
measure_angle_between_vectors(c(1, 0, 0), c(1, 0, 0)) # 0

}
\seealso{
\code{\link[=radians_to_degrees]{radians_to_degrees()}}, \code{\link[=degrees_to_radians]{degrees_to_radians()}}
}
