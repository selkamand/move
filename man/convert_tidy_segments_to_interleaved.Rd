% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rglhelpers.R
\name{convert_tidy_segments_to_interleaved}
\alias{convert_tidy_segments_to_interleaved}
\title{Convert tidy segment data to interleaved point format}
\usage{
convert_tidy_segments_to_interleaved(
  data,
  coord = c("x", "y", "z"),
  end_suffix = "end"
)
}
\arguments{
\item{data}{A data frame containing segment start and end coordinates.}

\item{coord}{Character vector of coordinate names (default: \code{c("x", "y", "z")}).}

\item{end_suffix}{String suffix appended to coordinate names for the end point
(default: \code{"end"}).}
}
\value{
A data frame with one row per point, containing:
\itemize{
\item \code{point}: \code{"start"} or \code{"end"}
\item the coordinate columns (\code{coord})
\item any additional metadata replicated for both points.
}
}
\description{
Takes a data frame / matrix of line segments where each row defines a start and end point
(e.g. \code{x}, \code{y}, \code{z}, \code{xend}, \code{yend}, \code{zend}) and returns a long-form, interleaved
data frame where each segment contributes two rows — one for the start and one for
the end point — preserving any non-coordinate metadata.
}
\examples{
df <- data.frame(x = 0, y = 0, z = 0, xend = 1, yend = 1, zend = 1, id = 1)
convert_tidy_segments_to_interleaved(df)

}
\seealso{
\code{\link[=geom_segment]{geom_segment()}} for ggplot2-style segment data.
}
